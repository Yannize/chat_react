let chat;_ee0‍.w("./controllers/chat",[["default",["chat"],function(v){chat=v}]]);const app = require('express')();
const http = require('http').createServer(app);

app.get('/', (req, res) => {
  res.send('Yoooo !');
});

http.listen(3001, () => {
  console.log(`listening to http://localhost:3001`);
});

chat(http);
